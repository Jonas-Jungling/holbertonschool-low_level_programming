#include "main.h"
#include <stdlib.h>
/**
* _abs - Entry point
* description: compute the absolute value of an integer
*@n: The value to check
* Return: Absolute value
*/

int _abs(int n)
{
	return (abs(n));
}
